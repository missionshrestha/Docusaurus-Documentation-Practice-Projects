---
id: cli-usage
title: CLI Usage
sidebar_label: CLI Usage
description: Learn the core TaskForge CLI commands to manage projects and tasks.
---

The TaskForge CLI lets you manage projects and tasks directly from your terminal.

:::info
Run `taskforge --help` at any time to see the full list of available commands and options.
:::

## CLI demo

Here's a quick demo of the CLI in action:

![TaskForge CLI Demo](../assets/cli-demo.gif)

## Global options

Common flags that work with most commands:

```bash
taskforge --help
taskforge --version
taskforge --config ./taskforge.config.json

---
TODO: content (how to use the TaskForge CLI)
